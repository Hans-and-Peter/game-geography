{
  "containerDefinitions": [
    {
      "memory": 300,
      "portMappings": [
        {
          "hostPort": 8080,
          "containerPort": 8080,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "name": "hans-and-peter-game-geography",
      "image": "${docker.hub.user}/${project.groupId}:${project.version}"
    }
  ],
  "family": "hans-and-peter-game-geography"
}
